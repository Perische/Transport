# MoveIt Backend

The MoveIt backend is a REST API built with Python and Flask.

## Technologies

- Python
- Flask
- Flask-SQLAlchemy
- Flask-CORS
- SQLite
- Werkzeug

## Setup

Install the required packages:

```bash
pip install -r requirements.txt